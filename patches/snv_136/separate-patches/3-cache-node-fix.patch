--- a/common.pdesc
+++ b/common.pdesc
@@ -35,6 +35,7 @@
     compatible = { "SUNW,UltraSPARC-T1", "SUNW,sun4v-cpu", "sun4v" }; \
     max-#tsb-entries = 0x2; \
     back -> cpus; \
+    fwd -> cache; \
     }

 node options XXoptions {
--- a/1up.pdesc
+++ b/1up.pdesc
@@ -45,3 +45,7 @@
 	fwd -> cpu0;
 	back -> root;
 }
+
+node cache cache {
+	back -> cpu0;
+}
